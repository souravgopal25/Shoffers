<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shoffers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".mens.MenShirt"></activity>
        <activity android:name=".women.WomenSelection" />
        <activity android:name=".child.ChildSelection" />
        <activity android:name=".mens.MenSelection" />
        <activity
            android:name=".navdrawer"
            android:label="@string/title_activity_navdrawer"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <activity android:name=".Signup" />
        <activity android:name=".Login" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>